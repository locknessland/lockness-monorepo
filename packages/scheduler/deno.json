{
    "name": "@lockness/scheduler",
    "description": "Declarative cron-based task scheduling with the @Schedule decorator, presets, retries and graceful stop",
    "version": "0.2.0",
    "license": "MIT",
    "exports": "./mod.ts",
    "tasks": {
        "test": "deno test -A tests/",
        "test:watch": "deno test -A --watch tests/"
    },
    "publish": {
        "exclude": [
            "tests/"
        ]
    },
    "imports": {
        "@std/assert": "jsr:@std/assert@1",
        "@std/testing": "jsr:@std/testing@^1.0.0",
        "@lockness/contract": "jsr:@lockness/contract@^0.2.0",
        "@lockness/container": "jsr:@lockness/container@^0.2.0"
    }
}
