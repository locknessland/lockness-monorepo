import { Database } from '@lockness/drizzle'
import { container } from '@lockness/core'

export class {{className}}Seeder {
    private database: Database

    constructor() {
        this.database = container.get<Database>(Database)
    }

    async run(): Promise<void> {
        console.log('🌱 Running {{className}}Seeder...')

        // Example: insert literal seed data
        // await this.database.db.insert(users).values([
        //     { email: 'admin@example.com' },
        //     { email: 'user@example.com' },
        // ])

        // Or seed from a factory (realistic bulk data — dev/test tooling):
        // await new UserFactory().createMany(10)

        console.log('✅ {{className}}Seeder completed')
    }
}
