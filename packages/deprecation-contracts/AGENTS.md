# `@lockness/deprecation-contracts` — agent brief

A convention for announcing deprecations: `trigger.ts` raises a notice,
`collector.ts` accumulates them, `formatter.ts` renders them. Standalone — it
imports no other Lockness package, so anything can depend on it.

User-facing documentation: [README.md](README.md) ·
[docs/DOCS.md](docs/DOCS.md). This brief does not repeat it.

## Invariants

- **The dependency contract above is binding.** Importing anything outside it
  fails `deno task deps:analyze`, and the failure is a design question, not a
  lint to silence.

_Add the domain invariants — what must stay true inside this package, and what
breaks when it does not. A statement that could have been guessed from the file
names does not belong here._

## Dependency contract

<!-- generated:deps -->

| Direction                                      | Packages                                 |
| :--------------------------------------------- | :--------------------------------------- |
| Imports (static)                               | —                                        |
| Imports (soft, via `tryImportOptionalPackage`) | —                                        |
| Imported by                                    | —                                        |
| **Must never import**                          | nothing — no package depends on this one |

Enforced by `deno task deps:analyze` against `deps.policy.jsonc`. A soft edge is
deliberately **not** declared in this package's `deno.json`: the consuming
application installs it, or the feature stays off.

<!-- /generated:deps -->

## Public surface

<!-- generated:surface -->

| Kind      | Exports                                                                                                                                                                                                                                                                   |
| :-------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| function  | `Deprecated`, `buildFullMessage`, `createEntry`, `createHandler`, `formatMessage`, `getCollector`, `getConfig`, `hasCollector`, `isStrictMode`, `notifyCollector`, `registerCollector`, `shouldIgnore`, `triggerDeprecation`, `triggerWithHandler`, `unregisterCollector` |
| interface | `DeprecationCollector`, `DeprecationConfig`, `DeprecationEntry`, `DeprecationHandler`, `DeprecationOptions`                                                                                                                                                               |
| reference | `registerDeprecationCollector`                                                                                                                                                                                                                                            |
| variable  | `defaultHandler`                                                                                                                                                                                                                                                          |

Anything not listed is internal and free to change.

<!-- /generated:surface -->

## Where to work

| Concern                            | Path            |
| ---------------------------------- | --------------- |
| Raising a deprecation              | `trigger.ts`    |
| Decorator form                     | `decorators.ts` |
| Aggregation for the devtools panel | `collector.ts`  |
| Project installer                  | `install.ts`    |

## Pitfalls

- Notices are collected in memory per process; a long-running server accumulates
  them indefinitely unless the collector is cleared.
- The devtools deprecation panel reads this collector. Changing the record shape
  breaks `packages/devtools/ui/panels/Deprecations.tsx` silently.

## Tests

<!-- generated:tests -->

1 test file for 9 source files:

- `packages/deprecation-contracts/tests/deprecation.test.ts`

<!-- /generated:tests -->

## Before you call it done

<!-- generated:gate -->

The framework-wide gate, from the repository root:

```bash
deno fmt && deno lint && deno check && deno task test
deno task deps:analyze     # cycles, declaration drift, tier policy
deno task agents:brief     # refresh this file's generated blocks
```

Then, specific to this package: run its 1 test file directly —

```bash
deno test -A packages/deprecation-contracts/
```

<!-- /generated:gate -->

---

_Framework-wide rules live in the root [AGENTS.md](../../AGENTS.md). The
dependency contract, public surface and test sections are generated by
`deno task agents:brief` — edit the code, not those blocks._
