{
    "name": "@lockness/container",
    "version": "0.3.0",
    "license": "MIT",
    "exports": "./mod.ts",
    "tasks": {
        "test": "deno test -A tests/",
        "test:watch": "deno test -A --watch tests/"
    },
    "publish": {
        "exclude": [
            "tests/"
        ]
    },
    "imports": {
        "@std/assert": "jsr:@std/assert@1",
        "@lockness/contract": "jsr:@lockness/contract@^0.3.0"
    },
    "description": "Lightweight dependency injection container with singleton management and property injection decorators"
}
