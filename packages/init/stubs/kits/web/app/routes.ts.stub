/**
 * Controllers Registry
 *
 * The explicit list used in production and by `deno task compile`. In
 * development the kernel discovers `app/controller/` instead.
 *
 * Regenerate it with `deno task routes:generate`.
 */

import { AppController } from '@controller/app_controller.tsx'
import { AuthController } from '@controller/auth_controller.tsx'

export const controllers = [
    AppController,
    AuthController,
]
