/**
 * Application Configuration
 *
 * Central export point. The slim kit ships three modules; add a namespace here
 * as you add the package that needs it — `database` for `@lockness/drizzle`,
 * `session` for `@lockness/session`, and so on.
 *
 * @module config
 */

import { appConfig } from './app.ts'
import { compileConfig } from './compile.ts'
import { listeners } from './listeners.ts'

/** Centralized configuration object with typed namespaces. */
export const config = {
    app: appConfig,
    compile: compileConfig,
    listeners,
} as const

/** Check if running in development mode */
export const isDevelopment = config.app.env === 'development'

/** Check if running in production mode */
export const isProduction = config.app.env === 'production'
