{
    "name": "@lockness/testing",
    "license": "MIT",
    "exports": "./mod.ts",
    "tasks": {
        "test": "deno test -A tests/",
        "test:watch": "deno test -A --watch tests/"
    },
    "publish": {
        "exclude": [
            "tests/"
        ]
    },
    "imports": {
        "@std/assert": "jsr:@std/assert@1",
        "@lockness/hono": "jsr:@lockness/hono@^0.2.0",
        "hono": "jsr:@lockness/hono@^0.2.0",
        "@lockness/auth": "jsr:@lockness/auth@^0.2.0"
    },
    "description": "Internal, unpublished test-support harness — HTTP test client, actingAs, fakes and DB assertions. Test-only; never imported at runtime."
}
