# `@lockness/testing` — agent brief

Internal, **test-only** helpers shared by the monorepo's own test suites — an
HTTP client over `app.request`, an `actingAs`/`fakeUser` identity pair, and an
in-memory `FakeTable`. It is **never published** and must never be imported by
runtime code.

## Invariants

- **The dependency contract above is binding.** Importing anything outside it
  fails `deno task deps:analyze`, and the failure is a design question, not a
  lint to silence.
- **Runtime code never imports this package.** Consumers touch it only from
  `tests/` (which `scripts/deps_analyzer.ts` excludes from the graph), so it
  creates no runtime edge. A non-test import is a defect.
- **Unpublished on purpose.** `deno.json` carries no `version` field; the
  package stays out of the publish set. This is what makes `actingAs` safe — it
  can never reach a consumer runtime as an auth-bypass primitive.
- **`actingAs` sets identity on the request context only.** It never mints a
  real session or token, and never writes to a session/user store.
- **Fixtures are synthetic.** `fakeUser` and every helper use placeholder values
  only — never a real credential.

## Dependency contract

<!-- generated:deps -->

| Direction                                      | Packages                                   |
| :--------------------------------------------- | :----------------------------------------- |
| Imports (static)                               | `auth` _(type-only)_, `hono` _(type-only)_ |
| Imports (soft, via `tryImportOptionalPackage`) | —                                          |
| Imported by                                    | —                                          |
| **Must never import**                          | nothing — no package depends on this one   |

Enforced by `deno task deps:analyze` against `deps.policy.jsonc`. A soft edge is
deliberately **not** declared in this package's `deno.json`: the consuming
application installs it, or the feature stays off.

<!-- /generated:deps -->

## Public surface

<!-- generated:surface -->

| Kind      | Exports                                           |
| :-------- | :------------------------------------------------ |
| class     | `FakeTable`                                       |
| function  | `actingAs`, `fakeUser`, `testClient`              |
| interface | `RequestableApp`, `TestClient`, `TestRequestInit` |
| typeAlias | `Row`                                             |

Anything not listed is internal and free to change.

<!-- /generated:surface -->

## Where to work

- HTTP client (`testClient`, request building) → `http_client.ts`
- Identity helpers (`actingAs`, `fakeUser`) → `acting_as.ts`
- DB assertions (`FakeTable`) → `db_assertions.ts`
- Public surface barrel → `mod.ts`

## Pitfalls

- **Adding a helper that needs a new package widens the allow-set, not the
  imports.** The deps.policy entry already permits auth/session/drizzle/hono/
  contract; anything beyond that is a deliberate `chore(deps)` change, and it
  must stay reachable only from consumers' `tests/`.

## Tests

<!-- generated:tests -->

1 test file for 4 source files:

- `packages/testing/tests/harness.test.ts`

<!-- /generated:tests -->

## Before you call it done

<!-- generated:gate -->

The framework-wide gate, from the repository root:

```bash
deno fmt && deno lint && deno check && deno task test
deno task deps:analyze     # cycles, declaration drift, tier policy
deno task agents:brief     # refresh this file's generated blocks
```

Then, specific to this package: run its 1 test file directly —

```bash
deno test -A packages/testing/
```

<!-- /generated:gate -->

---

_Framework-wide rules live in the root [AGENTS.md](../../AGENTS.md). The
dependency contract, public surface, tests and closing gate are generated by
`deno task agents:brief` from the code itself — fix the code, not those blocks.
Everything else is hand-written and preserved._
