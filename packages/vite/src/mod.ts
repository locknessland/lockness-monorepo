/**
 * @fileoverview Internal barrel for `@lockness/vite`. Re-exported by the
 * package's public `mod.ts`.
 *
 * Skeleton only (#105) — implementations land in #106–#113.
 *
 * @module @lockness/vite/src
 */

// TODO(#106–#113): export the lockness() factory (aggregate root), the config
// helper, the asset helper, the ManifestReader and DEFAULTS.
export {}
