/**
 * @fileoverview Shared, pure-data kernel for `@lockness/vite` — the single home
 * for DEFAULTS (configurable paths), the watcher glob sets, and the Tailwind CLI
 * invocation (plan §5 decision table). No I/O lives here.
 *
 * Skeleton only (#105) — DEFAULTS and the glob sets are populated by #107.
 *
 * @module @lockness/vite/shared
 */

// TODO(#107): DEFAULTS, SERVER_RELOAD_GLOBS, the CSS glob set (server-reload
// wins on overlap), and TAILWIND_CLI.
export {}
