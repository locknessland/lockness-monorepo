/**
 * @fileoverview Demo client entry. In a real app this hydrates islands or wires
 * client behaviour; the demo keeps it minimal — its job is to prove that the
 * client entry is resolved and bundled by @lockness/vite (build → manifest).
 *
 * @module demo/client
 */
import './view/assets/app.css'

console.info('[lockness-vite-demo] client entry loaded')
