/**
 * @fileoverview Public surface of `@lockness/vite` — the Deno-native Vite
 * integration for Lockness (dev server bridge, Deno specifier resolver, and a
 * manifest-aware asset helper).
 *
 * Skeleton only (#105): the exports below are filled by the epic's later
 * children (#106–#113). Keeping the surface here from the start lets those
 * children land in parallel without renegotiating the public API.
 *
 * @module @lockness/vite
 */

// TODO(#106–#113): re-export lockness(), defineViteConfig(), viteAssets(),
// LocknessViteConfig, ViteAssetsTagResult and DEFAULTS from ./src/mod.ts.
export {}
