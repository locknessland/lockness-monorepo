{
    "name": "@lockness/vite",
    "version": "0.2.0",
    "license": "MIT",
    "exports": "./mod.ts",
    "//": "hard-rule-2 exception: Vite has no JSR build, so `vite` is the ONE npm: specifier this package declares (the build tool it integrates). Rationale is also recorded in deps.policy.jsonc under \"vite\".",
    "tasks": {
        "test": "deno test -A tests/",
        "test:watch": "deno test -A --watch tests/"
    },
    "publish": {
        "exclude": [
            "tests/",
            "demo/"
        ]
    },
    "imports": {
        "@std/assert": "jsr:@std/assert@1",
        "@std/path": "jsr:@std/path@1",
        "vite": "npm:vite@^8"
    },
    "description": "Deno-native Vite integration for Lockness — dev server bridge, Deno specifier resolver, and a manifest-aware asset helper"
}
