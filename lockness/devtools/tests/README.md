# Devtools Tests

Run tests:

```bash
deno test -A lockness/devtools/tests/
```

Run with coverage:

```bash
deno test -A --coverage=coverage lockness/devtools/tests/
deno coverage coverage --lcov
```
