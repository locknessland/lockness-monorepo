import { Controller, Get, Context } from 'lockness/core'
import { {{ viewName }} } from '@view/pages/{{ route }}.tsx'

@Controller('/{{ route }}')
export class {{ className }}Controller {
    @Get('/', { name: '{{ route }}.index' })
    index(c: Context) {
        return c.html(<{{ viewName }} />)
    }
}
