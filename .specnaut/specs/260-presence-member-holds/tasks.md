# Tasks: Roster slots held per instance, presence announced per member

**Plan**: `.specnaut/specs/260-presence-member-holds/plan.md` (approved 2026-09-15) | **Backlog items**:
[#344 — Realtime: presence `joined` / `left` are announced per connection, not per member](https://github.com/locknessland/lockness-monorepo/issues/344) ·
[#345 — Realtime: one member on two instances — the Redis roster slot is last-writer-wins](https://github.com/locknessland/lockness-monorepo/issues/345)

TDD is mandatory (constitution): every behaviour task starts with a witness proven red on the
current code. Every task that touches a rule in the plan's 🔒 decision table names that rule's home
— the decision may not land anywhere else. Witness and mutant ids (W#, M#) are the dispositions'
(comments on #344 and #345); `345-W` and `344-W` disambiguate.

## Phase 1: Setup — test infrastructure

- [X] T001 [P] Lua evaluator subset in `packages/redis/tests/lua_eval.ts` (home of the subset row): `==` only, **type-strict** (`'1' == 1` is false); numeric literals are numbers in comparisons; `false` literal; nested `if <operand> == <operand> then … end`; `return` as the last statement of a block; `[n]` on a local; still throw on anything else (`~=`, `>`, `and`, loops). `LuaValue` doc: a nil reply reaches Lua as `false`. Rows in `packages/redis/tests/lua_eval.test.ts` (including `'1' == 1` false, nested return, local index); "Supported:" docstring; `packages/redis/AGENTS.md`. Run `live_conformance_285` after (its `:125` anchor moves — T018). Own `test(redis)` commit.
- [X] T002 [P] `packages/realtime/tests/fake_redis.ts`: `HGET key field` (nil when absent, arity checked); the EVAL bridge (`:584`) converts a nil reply to Lua `false` (home of that rule — no per-command special case). Rows in `fake_redis_280` / `fake_redis_conformance.test.ts`: `HGET` missing field is `false` inside a script; `HRANDFIELD k 1 WITHVALUES` returns `[f, v]`; an emptied hash no longer `EXISTS`; `HSET` returns 1 new / 0 update.

## Phase 2: Foundational — witnesses red, then the seam

- [X] T003 Witnesses, proven red on the current code (save the red run output to the scratchpad; W7s are green today and stay green):
  - `packages/realtime/tests/roster_holders_345.test.ts` — two `RedisBroadcastDriver`s on one `FakeRedis`: 345-W1 A,B hold 7, A releases → 7 still read, `total` 1; 345-W2 B writes last, A's liveness lapses, B reconciles → 7 still there; 345-W2′ A writes last → 7 with B's info; 345-W3 both release → gone and `EXISTS holders` = 0; 345-W4 two interleaved sweeps of A → 7 still there; 345-W5 info exact with 2 holders, with 3 releasing a non-shown holder leaves info unchanged; 345-W6 A swept then releases → B untouched; 345-W7 legacy field without holders key deleted by a sweep; 345-W8 one instance holding 7 and 8 releases 8 → 7 still there; S1b a hold on a never-registered instance is in the instances set; S1c a hold landing between a sweep's `SMEMBERS` and its end stays in the owned set. Manager row: two `ChannelManager`s, id 7 on each; A unsubscribes → a joiner on B sees 7 in `here`; then A dies → still there. Invariant helper: field exists ⇔ holders ≥ 1 after every step.
  - `packages/realtime/tests/presence_member_transitions_344.test.ts` — 344-W1 memory, c1+c2 as 7 → exactly one `joined`, one `presence-join`; 344-W2 c1 leaves, c2 in → no `left` / `presence-leave`; 344-W3 c2 leaves → exactly one `left`; 344-W4 W1–W3 roster-less; 344-W5 two managers on one FakeRedis → one `presence-join` total, A leaves → nothing, B leaves → one `presence-leave` from B; 344-W6 #330 gated rig, leave overtakes join → zero `presence-join`, zero `presence-leave` (observe control frames or a second manager — A-L4); 344-W7 hold lands first → `joined` then `left` (green today); 344-W8 c2 joins as 7 gated at `#watch`, c1 leaves, c2 leaves → exactly one `left`, from c2's queued write; 344-W9 c2 claims 7 while c1's arrival write is gated → neither receives `joined` for 7, **and the remote variant** (A5): a connection on B that claimed 7 never receives B's re-emit of A's `presence-join`; 344-W10 `presence-leave` publish rejects → `unsubscribe` resolves `'left'` with one WARN containing no member id and no `info` (S4), and a failing **release** still rejects; 344-W12 constructor throws once for a driver with `addMember`/`removeMember` (and with `listMembers` too), naming all of them.
- [X] T004 The seam in `packages/realtime/driver.ts` (home): `holdMember` / `releaseMember` optional on `BroadcastDriver`, required on `PresenceCapableDriver`; `RosterHold { readonly arrived }`, `RosterRelease { readonly gone }` with full JSDoc and `@example` (hold/release meaning, `arrived` only if no process held it, `gone` only if this process held it and none is left; a driver may not fake either); remove `addMember` / `removeMember`. Type exports in `packages/realtime/mod.ts`.
- [X] T005 [US5] `packages/realtime/manager.ts`: `presenceRoster` = `holdMember` + `releaseMember` + `readRoster` (home); `assertNotLegacyRosterDriver` collects `listMembers` / `addMember` / `removeMember` and throws **once** naming all present, citing the upgrade section by title (home; no item number). Turns 344-W12 green.

## Phase 3: US3 + US4 — Redis holds (P1/P2, #345)

- [X] T006 [US3] `packages/realtime/drivers/redis.ts`: `holdersKey(channel, id)` beside `presenceKey`/`ownedKey`, reusing `OWNED_SEP`, JSDoc naming `#assertUsableChannel` (FR-003, home); `HOLD_MEMBER_SCRIPT` and `RELEASE_MEMBER_SCRIPT` **byte-for-byte as plan §6** (replacing `ADD_MEMBER_SCRIPT` / `REMOVE_MEMBER_SCRIPT`), JSDoc key legends (hold four keys, release three), no `EXPIRE`; `holdMember` / `releaseMember` issue one `EVAL` each and decode through ONE private decoder (1 → true, 0 → false, else throw — FR-004a). `readRoster` untouched (FR-007).
- [X] T007 [US3] `#sweepInstance(deadId)` in `packages/realtime/drivers/redis.ts`: one `RELEASE_MEMBER_SCRIPT` per owned entry with `ownedKey(deadId)` and `deadId`, return ignored; then `SREM instances deadId`; **no `DEL owned`** (S1c); keep the WARN. No raw presence `HDEL` remains. Turns 345-W1…W8, S1b, S1c and the manager row green.
- [X] T008 [P] [US1] `packages/realtime/drivers/memory.ts`: `holdMember` → `{ arrived: !members.has(key) }` before the set; `releaseMember` → `{ gone: members.delete(key) }`. Contract row: add twice, release once → gone.

## Phase 4: US1 + US2 + US6 — announcements from the queue (P1, #344)

- [X] T009 [US1] `packages/realtime/manager.ts` `#syncRosterMember(channel, origin: { clientId, member }): Promise<void>` (home of "who announces"): `desired` still from `#localRoster` inside the queue; hold or release; roster-less arrived/gone from private `#heldSlots` (updated before announcing); on `arrived` → `#announcePresence('joined', desired, origin)`, on `gone` → `#announcePresence('left', origin.member, origin)`. Update its three callers (join, #323 compensation, `unsubscribe`).
- [X] T010 [US1] NEW `#announcePresence` in `packages/realtime/manager.ts` (home of order/payload/WARN): emit locally, then `publishControl({ kind, target: origin.clientId, channel, member })`; a publish or `encode` failure → one WARN with channel (`safeForLog`) + action + `renderError(error)` only, never rethrown. `emitPresence(channel, frame, { exceptMemberId })` (home of the exclusion) skips every local subscriber whose presence entry has that `String(id)`; used by `#announcePresence` for `joined` and by `handleControl`'s `presence-join` arm; `left` excludes nobody.
- [X] T011 [US2] [US6] `packages/realtime/manager.ts`: remove the emit/publish blocks from `#joinPresence` (`:1486-1529`) and `unsubscribe` (`:2050-2063`); remove the `applied === undefined` branch (a superseded join still returns `#closingRead`); `LeaveOutcome` JSDoc: `'left'` = a membership was removed here, not a frame sent. Turns 344-W1…W6, W8…W10 green; 344-W7 stays green.
- [X] T012 Migrate every remaining `addMember` / `removeMember` in `packages/realtime/tests/` (146 references, 27 files — enumerate with `grep -rln "addMember\|removeMember" packages/realtime/tests`): rename, return `{ arrived }` / `{ gone }`, keep each double's `source: 'authoritative'` / read-count assertion. **Intentionally inverted assertions** — change only with the reason in the test, each re-proven: `presence_join_rosterless_342.test.ts:178-182` (W2 → 0 `presence-leave`, per 344-W6); `roster_control_atomicity.test.ts:217-248` (newcomer's log is `[holdMember]`). **Sweep rule**: any other test with two connections on one member id, or an overtaken join, that counts `joined` / `left` / `presence-*` is listed in the commit body and re-proven. `prefix_anchoring.test.ts`: `PREFIX_MEMBERS` gains `holdersKey`; its canned `EVAL` reply (`:215`) decodes under FR-004a. Must stay green: `presence_join_compensation_323`, `presence_rejoin_327`, `subscribe_unsubscribe_race_330`, `presence_local_member_343`, `presence_eviction_334`, `churn_cost_329` (totals unchanged — SC-005).
- [X] T013 Gated live rows (`LOCKNESS_REDIS_INTEGRATION=1`) in `packages/realtime/tests/live_fake_conformance.test.ts` / `redis_broker_integration.test.ts`: `HGET` missing field is `false` in Lua; `HRANDFIELD k 1 WITHVALUES` shape; emptied holders hash gone; a copied entry reads back byte for byte; `EVAL` 1/0 decoded as boolean; `HSET` in a script returns 1 new / 0 update; 345-W1…W8 and the invariant on a real broker; 344-W5 counting control frames on a real broker.

## Phase 5: Batteries

- [x] T014 [P] NEW `packages/realtime/tests/mutations/presence_member_holds_345.ts`, each row proven live: unconditional presence `HDEL` in release (W1); sweep back to raw `HDEL` (W2); hold skips holders `HSET` (W1); release skips holders `HDEL` (W3); `shown == mine` guard dropped (W5); copy branch dropped (W2′/W5); holders key without the field (W8); sweep passes its own id instead of `deadId` (W2); hold skips `SADD instances` (S1b); sweep `DEL owned` restored (S1c); decoder treats any truthy reply as true (FR-004a row).
- [x] T015 [P] NEW `packages/realtime/tests/mutations/presence_member_transitions_344.ts`, each row proven live: M1 ignore `arrived` (W1); M2 ignore `gone` (W5, W6); M3 hold script drops `n == 1` (W5); M4 release drops `if mine == false then return 0 end` (W11 contract rows); M5 `#heldSlots` release always true (W4); M6 restore announcing from `unsubscribe` (W2); M7 `joined` excludes only `origin.clientId` (W9, successor to `presence_join_323` row at `:69-72`); M8 rethrow the publish error inside the queue (W10); M9 remove the legacy-name refusal (W12); M10 memory `arrived = true` (W1); WARN includes member id (S4); `handleControl` join arm without `exceptMemberId` (remote W9). 344-W11 contract rows (memory, fake, live): hold → true, hold again → false, release with another holder → false, last release → true, non-holder release on empty slot → false, two drivers interleaved.
- [x] T016 Re-anchor, run, and prove every row live — the 10 breaking anchors (plan §8): `presence_join_rosterless_342.ts` **retired** (all 3 rows), its reason carried in the file header to successors 344-W4/W6 via M2/M5 (`docs/testing.md` retired-row rule); `presence_join_323.ts` `:69-72` (→ M7), `:128-134`, `:153` (`holdMember`), `:187-205` (EVAL argument text, now four KEYS); `roster_sync_330.ts:61`; `presence_member_306.ts:90-91`; `live_conformance_285.ts:125`; `sweep_parse_316.ts` (sweep body); `prefix_288.ts` adds a `holdersKey` row. Re-run unchanged `presence_local_member_343`, `presence_eviction_334`, `presence_read_bound_341`, `roster_read_barrier_333`, `presence_snapshot_339`. No row may report a result on an anchor that no longer matches.

## Phase 6: Polish — docs and prose

- [X] T017 [P] `docs/realtime.md` (FR-014): "What a `joined` frame promises" 61–90 (per member; lost-reply `left`); failed-join path 737–744; roster section and ghost sweep 1048–1192 (replace "last write wins (#345)" and "Announcements are still per connection"; multi-instance guarantee; sweep = one release `EVAL` per entry); `evict` / `revokeChannel` wording; "Writing a presence driver" 1193–1232 (rename, return values, hold semantics, may not fake); Redis requirements: no TTL on roster keys → `noeviction` or `volatile-*`, `1 + k` entries per member; v0.4.0 intro 1585–1589; item 3 → "no migration step; one new key family" plus the rollback-then-re-upgrade `SCAN MATCH` + `UNLINK` procedure handling each key as opaque (P1, S2); **item 7** = the whole driver-author break (bounded read, hold/release, single refusal, Before/After table); **NEW item 8** = application-visible only (per-member frames, apps counting tabs lose that signal, `unsubscribe` no longer rejects on publish failure, cross-link item 4). `README.md` if it names the seam.
- [X] T018 [P] NEW `docs/adr/004-realtime-roster-slots-held-per-instance.md` (holders hash, hold/release, sweep = release, the holder that fills or empties a slot announces it, per-identity unique id makes `joined`/`left` truthful — #346, rejected alternatives with costs, residue); ADR 003 §3 / §6 / §7 amended and linked to 004 (lines 31, 32, 41–58, 86, 91–110).
- [X] T019 [P] In-code prose (FR-015): `manager.ts` 389–407, 749–750, 786–788, 1326–1328, 1355–1356, 1544–1545, 1788–1790, 1845–1877, 1977–1979, 1988–1992, 2071–2072, 2112–2114, 2237–2238, 2648–2654, 2703–2709; `redis.ts` 150–200, 1450–1500, 2199, 2230–2232 (line numbers at `70ec5462`, re-grep after T006–T011). `packages/realtime/AGENTS.md` pitfalls 145–156 and 262–269 rewritten, new pitfall "announcements live in the queue, never back in `subscribe` / `unsubscribe`"; regenerate with `deno task agents:brief`.
- [X] T020 Full gate: `deno fmt --check`, `deno lint`, `deno check`, `deno task deps:analyze`, `deno task agents:brief --check`, `deno task test`, `deno task test:redis` (live), every battery of T014–T016 (`live_conformance_285` with `LOCKNESS_REDIS_INTEGRATION=1`); FR-013 searches return only the legacy guard's probes and historical specs.

## Dependencies

T001, T002 → T003 → T004 → T005 → (T006 → T007), T008 → T009 → T010 → T011 → T012 → T013 → T014, T015, T016 → T017, T018, T019 → T020.

T004–T012 change one seam across source and tests; `deno check` stays red between T004 and T012,
so those tasks land in one working session and are committed together at merge squash. T001/T002,
T006-T007/T008, T014/T015 and T017/T018/T019 are the parallel groups.

## Implementation strategy

MVP checkpoint = Phase 3 (#345 on Redis green with the renamed seam, manager calls adapted without
announcement changes). Phase 4 then moves announcements into the queue (#344). Commit scopes at
merge: `test(redis)` (T001), `fix(345)` (T004 driver seam + T006–T008), `fix(344)` (T005, T009–T011),
`test(345)` / `test(344)` (T002, T003, T012–T016), `docs(344)` (T017–T019). The branch ships only
after T020; the MVP is a checkpoint inside the full path, not a fork.
